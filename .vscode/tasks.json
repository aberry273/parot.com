// Sass configuration
{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Sass Compile",
        "type": "shell",
        "command": "sass --pkg-importer=node --load-path=../../node_modules/@picocss/pico/scss/ --load-path=../../../node_modules/@picocss/pico/scss/ --watch main.scss main.css",
        "group": "build"
      }
    ]
  }